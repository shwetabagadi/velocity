package com.test.IBM;

import java.sql.Time;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;

import POM.FeedPage;
import POM.LoginPage;
import POM.LogoutSession;
import POM.MessengerSection;

public class Test123 
{
    public static void main( String[] args ) throws InterruptedException
    {	/*
    	//set properties of webdriver
    	System.setProperty("webdriver.chrome.driver","D:\\Selenium\\chromedriver_win32\\chromedriver.exe");
		WebDriver driver = new ChromeDriver();
		//open facebook application
		driver.get("https://www.facebook.com/");
		//login to facebook
		LoginPage loginpage = new LoginPage(driver);
		loginpage.sendUserName();
		loginpage.sendPasswordName();
		loginpage.clickOnLogin();
		Thread.sleep(2000);
		
		//search for profile
		FeedPage searchProfile = new FeedPage(driver);
		searchProfile.seachForProfile();
		
		//open messenger for perticular profile
		MessengerSection messengerb = new MessengerSection(driver);
		messengerb.openMessenger();
		
		//logout the session
		LogoutSession logoutsession = new LogoutSession(driver);
		logoutsession.logoutDropdown();
		
		
		
		
		
		
		
        */
    }
    
}
